{
  "name": "devops-e2e-pipeline-app",
  "version": "1.0.0",
  "description": "Sample task API used to demonstrate an end-to-end DevOps pipeline",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "lint": "eslint src test",
    "test": "jest --runInBand",
    "test:coverage": "jest --runInBand --coverage --coverageReporters=lcov --coverageReporters=text"
  },
  "dependencies": {
    "express": "^4.19.2",
    "prom-client": "^15.1.3"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  }
}
